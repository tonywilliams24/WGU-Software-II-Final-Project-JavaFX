package model;

import javafx.collections.FXCollections;
import javafx.collections.ObservableList;

public class Inventory {
    public static TableList<Appointment> appointmentList = new TableList<>();
    public static TableList<Contact> contactList = new TableList<>();
    public static TableList<Country> countryList = new TableList<>();
    public static TableList<Customer> customerList = new TableList<>();
    public static TableList<First_Level_Division> first_level_divisionList = new TableList<>();
    public static TableList<User> userList = new TableList<>();
}
